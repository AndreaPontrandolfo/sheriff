{
  "$schema": "https://turbo.build/schema.json",
  "globalDependencies": ["**/.env.*local"],
  "pipeline": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["**/.docusaurus/**/*"]
    },
    "lint": {},
    "start": {
      "cache": false,
      "persistent": true,
      "outputs": ["**/src/rules.json", "**/.docusaurus/**/*"]
    },
    "rulesgen": {
      "outputs": ["./src/rules.json"]
    },
    "publint": {
      "outputs": ["dist/**"],
      "dependsOn": ["build"]
    },
    "clean": {
      "cache": false
    }
  }
}
